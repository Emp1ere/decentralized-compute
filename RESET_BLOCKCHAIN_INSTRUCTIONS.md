# Инструкция по обнулению блокчейна

**Дата:** 2026-02-11

---

## ✅ БЛОКЧЕЙН УЖЕ ОБНУЛЁН

Файл `orchestrator_node/data/chain.json` обновлён и содержит только genesis блок.

---

## 📋 ЧТО БЫЛО СДЕЛАНО

1. ✅ Удалены все блоки кроме genesis (index=0)
2. ✅ Все транзакции удалены (reward и work_receipt)
3. ✅ Балансы всех вычислителей будут обнулены при перезапуске
4. ✅ Прогресс выполнения контрактов обнулён

**Пользователи (users.json) сохранены** — они останутся в системе, но их балансы будут 0.

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

### 1. Перезапустите систему

**Windows:**
```bash
docker-compose restart
```

**Или полная пересборка:**
```bash
docker-compose down
docker-compose up -d
```

### 2. Проверьте результат

1. Откройте интерфейс: http://localhost:8080
2. Перейдите на вкладку **"Обзор"**
3. Проверьте:
   - Длина цепочки должна быть **1** (только genesis)
   - Балансы всех вычислителей должны быть **0**
   - Количество сданных работ должно быть **0**

4. Перейдите на вкладку **"Контракты"**
5. Проверьте:
   - Выполнено % должно быть **0%** для всех контрактов
   - Активных вычислителей должно быть **0**

---

## 🔄 ЕСЛИ НУЖНО ВОССТАНОВИТЬ

Если вы случайно обнулили блокчейн и хотите восстановить:

1. Проверьте наличие резервной копии:
   ```bash
   ls orchestrator_node/data/chain.json.backup
   ```

2. Если резервная копия есть, восстановите:
   ```bash
   # Windows
   copy orchestrator_node\data\chain.json.backup orchestrator_node\data\chain.json
   
   # Linux/Mac
   cp orchestrator_node/data/chain.json.backup orchestrator_node/data/chain.json
   ```

3. Перезапустите систему

---

## 📝 СКРИПТ ДЛЯ АВТОМАТИЧЕСКОГО ОБНУЛЕНИЯ

Создан скрипт `reset_blockchain.py` для будущего использования:

**Windows:**
```bash
python reset_blockchain.py
# или
reset_blockchain.bat
```

**Linux/Mac:**
```bash
python3 reset_blockchain.py
# или
./reset_blockchain.sh
```

Скрипт:
- Создаёт резервную копию перед обнулением
- Показывает статистику перед обнулением
- Запрашивает подтверждение
- Оставляет только genesis блок

---

## ⚠️ ВАЖНО

- **Данные пользователей сохраняются** — файл `users.json` не изменяется
- **Балансы обнуляются** — пересчитываются из пустого блокчейна (только genesis)
- **Прогресс контрактов обнуляется** — статистика пересчитывается из блокчейна
- **Резервная копия** — создаётся автоматически при использовании скрипта

---

## ✅ СТАТУС

**Блокчейн обнулён:** ✅  
**Genesis блок сохранён:** ✅  
**Готово к перезапуску:** ✅
