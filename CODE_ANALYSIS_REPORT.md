# Отчёт по анализу кода проекта distributed-compute

**Дата анализа:** 2026-02-11  
**Анализируемые аспекты:**
1. Несогласованность
2. Лишние куски кода
3. Критические уязвимости
4. Соответствие идее проекта

---

## 1. НЕСОГЛАСОВАННОСТЬ

### 1.1. Несогласованность в маршрутизации балансировщика

**Проблема:** В `nginx.conf` все критичные операции (`/submit_work`, `/get_task`, `/register`, `/me`, `/get_balance`, `/auth/`, `/run_worker`, `/worker_progress`) направлены на `node1_only`, но при этом декларируется децентрализация.

**Местоположение:** `nginx_loadbalancer/nginx.conf`

**Детали:**
- Комментарий говорит: "Операции с аккаунтом и балансом всегда идут на node_1"
- Но `/submit_work` и `/get_task` тоже идут на node_1, хотя это не операции с аккаунтом
- Это создаёт централизацию трафика на один узел

**Рекомендация:** Пересмотреть маршрутизацию — возможно, `/get_task` и `/submit_work` можно распределять между узлами, оставив только операции с балансом на node_1.

---

### 1.2. Несогласованность в использовании лидера

**Проблема:** Функция `get_current_leader()` реализована и используется в `/metrics`, но **не используется** в логике создания блоков (`submit_work`).

**Местоположение:** `orchestrator_node/app.py:136-148, 371-549`

**Детали:**
- Лидер определяется детерминированно по хешу последнего блока
- Но в `submit_work` блок создаёт тот узел, который принял запрос, независимо от того, является ли он лидером
- Комментарий в коде (строка 497-500) говорит: "узел, принявший submit_work, всегда создаёт блок сам"

**Рекомендация:** Либо использовать лидера для создания блоков (ротация), либо убрать функцию лидера из метрик, если она не используется.

---

### 1.3. Несогласованность в обработке ошибок при синхронизации

**Проблема:** В `sync_chain_from_peer()` и `sync_pending_from_peer()` ошибки логируются как `debug` или `warning`, но не всегда обрабатываются единообразно.

**Местоположение:** `orchestrator_node/app.py:174-225`

**Детали:**
- `sync_pending_from_peer()` ловит `requests.RequestException` и логирует как `debug`
- `sync_chain_from_peer()` ловит `requests.RequestException` и логирует как `warning`
- Разный уровень логирования для похожих операций

**Рекомендация:** Унифицировать обработку ошибок синхронизации.

---

### 1.4. Несогласованность в проверке nonce

**Проблема:** В `contracts.py` проверка `nonce` выполняется дважды в разных местах для астрофизических контрактов.

**Местоположение:** `orchestrator_node/contracts.py:68-99`

**Детали:**
- Строка 68: проверка `if nonce is None or nonce == ""`
- Строка 98: снова проверка `if nonce is None or nonce == ""`
- Вторая проверка выполняется после блока астрофизических задач и никогда не достигается

**Рекомендация:** Удалить дублирующую проверку nonce после блока астрофизических задач.

---

### 1.5. Несогласованность в возвращаемых значениях

**Проблема:** `add_block_from_peer()` возвращает кортеж `(bool, str|None)`, но в некоторых местах используется только первый элемент.

**Местоположение:** `orchestrator_node/blockchain.py:268-307`, `orchestrator_node/app.py:838`

**Детали:**
- В `blockchain.py:276` возвращается `True, None` при идемпотентности
- В `app.py:838` используется `ok, err = blockchain.add_block_from_peer(data)`
- Но `err` не всегда используется для логирования

**Рекомендация:** Улучшить использование возвращаемых значений для более информативного логирования.

---

## 2. ЛИШНИЕ КУСКИ КОДА

### 2.1. Неиспользуемая функция `get_leader_url()`

**Проблема:** Функция `get_leader_url()` определена, но нигде не используется.

**Местоположение:** `orchestrator_node/app.py:151-172`

**Детали:**
- Функция создана для получения URL лидера
- Но в коде нет вызовов этой функции
- Возможно, планировалось использовать для ротации лидера, но не реализовано

**Рекомендация:** Удалить функцию или реализовать её использование.

---

### 2.2. Закомментированный код в docker-compose.yml

**Проблема:** Закомментирован узел 3 и `restart: unless-stopped` для всех сервисов.

**Местоположение:** `docker-compose.yml:35, 60, 75, 92, 107, 120`

**Детали:**
- Узел 3 закомментирован (строки 62-75)
- `restart: unless-stopped` закомментирован для всех сервисов
- Это может быть намеренно (для разработки), но создаёт неясность

**Рекомендация:** Либо раскомментировать для продакшена, либо добавить комментарий, почему закомментировано.

---

### 2.3. Неиспользуемый метод `mine_block()` в классе Block

**Проблема:** Метод `mine_block()` определён в классе `Block`, но не используется (PoUW не требует майнинга).

**Местоположение:** `orchestrator_node/blockchain.py:110-116`

**Детали:**
- Метод оставлен "для совместимости" (комментарий)
- Но при PoUW он никогда не вызывается
- Создаёт путаницу в коде

**Рекомендация:** Удалить метод или добавить явный комментарий, что он не используется при PoUW.

---

### 2.4. Дублирование логики проверки nonce

**Проблема:** В `contracts.py` проверка nonce выполняется несколько раз в разных ветках.

**Местоположение:** `orchestrator_node/contracts.py:50-104`

**Рекомендация:** Вынести проверку nonce в начало функции `verify()`.

---

### 2.5. Неиспользуемый импорт в worker.py

**Проблема:** В `client_worker/worker.py` импортируется `requests.utils.quote`, но используется только в одном месте.

**Местоположение:** `client_worker/worker.py:108`

**Детали:**
- Можно использовать f-string для URL без импорта `quote`
- Или использовать `urllib.parse.quote` из стандартной библиотеки

**Рекомендация:** Заменить на стандартную библиотеку или упростить формирование URL.

---

## 3. КРИТИЧЕСКИЕ УЯЗВИМОСТИ

### 3.1. Централизация создания блоков ⚠️ КРИТИЧНО

**Проблема:** Все блоки создаются на узле, который принял `submit_work`. При маршрутизации через балансировщик все запросы идут на `node_1`, что создаёт централизацию.

**Местоположение:** `orchestrator_node/app.py:371-549`, `nginx_loadbalancer/nginx.conf`

**Риск:** Высокий

**Детали:**
- В `nginx.conf` все `/submit_work` идут на `node1_only`
- Это означает, что node_1 создаёт все блоки
- Нарушает принцип децентрализации

**Рекомендация:** 
- Распределить `/submit_work` между узлами через балансировщик
- Или реализовать механизм ротации лидера для создания блоков

---

### 3.2. Отсутствие валидации размера данных в submit_work

**Проблема:** Хотя есть `MAX_CONTENT_LENGTH = 16MB`, но нет проверки размера `result_data` в транзакции.

**Местоположение:** `orchestrator_node/app.py:23, 371-549`

**Риск:** Средний

**Детали:**
- `result_data` может быть очень большим (хеш 64 символа, но теоретически может быть больше)
- Большие транзакции могут забить память и замедлить синхронизацию

**Рекомендация:** Добавить проверку размера `result_data` (например, максимум 1KB для хеша).

---

### 3.3. Потенциальная утечка информации в ошибках

**Проблема:** В некоторых местах ошибки могут раскрывать внутреннюю структуру системы.

**Местоположение:** `orchestrator_node/app.py:518-519`

**Риск:** Низкий-Средний

**Детали:**
- `return jsonify({"error": "Cannot add transaction", "detail": str(e)}), 400`
- Может раскрывать внутренние детали реализации

**Рекомендация:** В продакшене не возвращать `detail` или логировать его отдельно.

---

### 3.4. Отсутствие защиты от переполнения pending при синхронизации

**Проблема:** При `sync_pending_from_peer()` транзакции добавляются без проверки общего лимита `MAX_PENDING_TOTAL`.

**Местоположение:** `orchestrator_node/app.py:174-203`

**Риск:** Средний

**Детали:**
- `blockchain.add_transaction(tx)` проверяет лимиты, но при синхронизации может быть добавлено много транзакций
- Если у пира 500 транзакций в pending, а у нас 0, мы можем превысить лимит

**Рекомендация:** Добавить проверку общего лимита перед добавлением транзакций из синхронизации.

---

### 3.5. Отсутствие проверки таймаутов при синхронизации

**Проблема:** `sync_chain_from_peer()` и `sync_pending_from_peer()` используют `PEER_REQUEST_TIMEOUT`, но нет проверки на слишком долгие запросы.

**Местоположение:** `orchestrator_node/app.py:174-225`

**Риск:** Низкий

**Рекомендация:** Добавить мониторинг времени синхронизации и предупреждения при долгих запросах.

---

### 3.6. Потенциальная проблема с race condition при пересылке блока

**Проблема:** В `receive_block()` блок пересылается пиру без проверки, был ли он уже переслан.

**Местоположение:** `orchestrator_node/app.py:827-853`

**Риск:** Низкий

**Детали:**
- Если два узла одновременно получают блок от третьего узла, они могут переслать его друг другу несколько раз
- Это создаёт лишний трафик

**Рекомендация:** Добавить проверку, откуда пришёл блок, чтобы не пересылать обратно отправителю.

---

### 3.7. Отсутствие проверки на циклическую пересылку блоков

**Проблема:** При топологии "цепочка" (node_1 ↔ node_2 ↔ node_3) блок может циклически пересылаться.

**Местоположение:** `orchestrator_node/app.py:840-850`

**Риск:** Низкий (только при 3+ узлах)

**Рекомендация:** Добавить проверку источника блока или использовать флаг "уже переслан".

---

## 4. СООТВЕТСТВИЕ ИДЕЕ ПРОЕКТА

### 4.1. Соответствие концепции PoUW ✅

**Статус:** Соответствует

**Детали:**
- Блоки создаются при верифицированной полезной работе (`submit_work` + `contract.verify()`)
- Нет бесполезного перебора nonce
- Ресурсы идут на полезные вычисления (астрофизика, симуляции)

---

### 4.2. Соответствие концепции децентрализации ⚠️ ЧАСТИЧНО

**Статус:** Частично соответствует

**Детали:**
- ✅ Два равноправных узла с синхронизацией
- ✅ Консенсус "longest valid chain"
- ✅ Синхронизация pending и цепочки
- ❌ Но создание блоков централизовано на node_1 из-за маршрутизации nginx

**Рекомендация:** Исправить маршрутизацию для истинной децентрализации.

---

### 4.3. Соответствие концепции распределённых вычислений ✅

**Статус:** Соответствует

**Детали:**
- Воркеры получают задачи через API
- Выполняют вычисления локально
- Сдают результаты и получают награды
- Система масштабируется горизонтально

---

### 4.4. Соответствие концепции блокчейна ✅

**Статус:** Соответствует

**Детали:**
- Цепочка блоков с хешами
- Транзакции (reward, work_receipt)
- Балансы пересчитываются из транзакций
- Персистентность на диск

---

### 4.5. Соответствие концепции безопасности ⚠️ ЧАСТИЧНО

**Статус:** Частично соответствует

**Детали:**
- ✅ Аутентификация по API-ключу
- ✅ Rate limiting
- ✅ Проверка nonce для защиты от подделки
- ✅ Защита от replay атак (проверка proof)
- ⚠️ Но есть централизация и потенциальные утечки информации

---

## ИТОГОВЫЕ РЕКОМЕНДАЦИИ

### Критические (исправить немедленно):

1. **Исправить маршрутизацию в nginx.conf** — распределить `/submit_work` между узлами
2. **Удалить дублирующую проверку nonce** в `contracts.py`
3. **Добавить проверку размера result_data** в `submit_work`

### Важные (исправить в ближайшее время):

4. **Удалить неиспользуемую функцию `get_leader_url()`** или реализовать её использование
5. **Улучшить обработку ошибок синхронизации** (унифицировать логирование)
6. **Добавить проверку общего лимита pending** при синхронизации

### Желательные (улучшения):

7. **Удалить неиспользуемый метод `mine_block()`** или добавить явный комментарий
8. **Улучшить использование возвращаемых значений** в `add_block_from_peer()`
9. **Добавить защиту от циклической пересылки блоков** при 3+ узлах

---

## ОБЩАЯ ОЦЕНКА

| Аспект | Оценка | Комментарий |
|--------|--------|-------------|
| **Несогласованность** | ⚠️ Средняя | Есть несколько мест с несогласованностью, но не критично |
| **Лишний код** | ✅ Низкая | Минимальное количество неиспользуемого кода |
| **Уязвимости** | ⚠️ Средняя | Есть критическая проблема с централизацией |
| **Соответствие идее** | ✅ Высокая | В целом соответствует концепции PoUW и распределённых вычислений |

**Общая оценка:** ⚠️ **ХОРОШО, но требует исправления централизации**

---

## ПРИОРИТЕТЫ ИСПРАВЛЕНИЙ

1. 🔴 **Критично:** Исправить централизацию создания блоков (nginx.conf)
2. 🟡 **Важно:** Удалить дублирование кода и несогласованности
3. 🟢 **Желательно:** Улучшить обработку ошибок и логирование
