# Инструкция по полной пересборке системы

**Дата:** 2026-02-11  
**После внесения изменений в код требуется полная пересборка**

---

## 🚀 БЫСТРЫЙ СПОСОБ (Рекомендуется)

### Windows:
```bash
# Остановить и удалить все контейнеры
docker-compose down

# Пересобрать и запустить всё заново
docker-compose up -d --build

# Или используйте скрипт:
start.bat
```

### Linux/Mac:
```bash
# Остановить и удалить все контейнеры
docker-compose down

# Пересобрать и запустить всё заново
docker-compose up -d --build

# Или используйте скрипт:
./start.sh
```

---

## 🔧 ПОЛНАЯ ПЕРЕСБОРКА (С очисткой)

Если нужно полностью очистить систему и пересобрать с нуля:

### Windows (PowerShell или CMD):
```bash
# 1. Остановить и удалить все контейнеры
docker-compose down

# 2. Удалить все образы проекта (опционально, но рекомендуется для чистой пересборки)
docker rmi distributed-compute-orchestrator_node_1 distributed-compute-orchestrator_node_2 distributed-compute-client_worker_1 distributed-compute-client_worker_2 2>$null

# 3. Удалить неиспользуемые образы (очистка)
docker image prune -f

# 4. Пересобрать все образы без кэша (чистая сборка)
docker-compose build --no-cache

# 5. Запустить систему
docker-compose up -d

# 6. Проверить статус
docker-compose ps
```

### Linux/Mac:
```bash
# 1. Остановить и удалить все контейнеры
docker-compose down

# 2. Удалить все образы проекта (опционально)
docker rmi distributed-compute-orchestrator_node_1 distributed-compute-orchestrator_node_2 distributed-compute-client_worker_1 distributed-compute-client_worker_2 2>/dev/null || true

# 3. Удалить неиспользуемые образы
docker image prune -f

# 4. Пересобрать все образы без кэша
docker-compose build --no-cache

# 5. Запустить систему
docker-compose up -d

# 6. Проверить статус
docker-compose ps
```

---

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ

### Шаг 1: Остановка текущей системы

```bash
docker-compose down
```

Это остановит и удалит все контейнеры, но сохранит:
- ✅ Данные в `orchestrator_node/data/` (блокчейн, пользователи)
- ✅ Образы Docker (если не удалять вручную)

### Шаг 2: Проверка изменённых файлов

Убедитесь, что все изменения сохранены:
- ✅ `nginx_loadbalancer/nginx.conf` — изменена маршрутизация
- ✅ `orchestrator_node/app.py` — добавлены комментарии и улучшения
- ✅ `orchestrator_node/contracts.py` — удалена дублирующая проверка
- ✅ `orchestrator_node/static/dashboard.html` — улучшения интерфейса

### Шаг 3: Пересборка образов

**Обычная пересборка (с кэшем):**
```bash
docker-compose build
```

**Полная пересборка (без кэша, рекомендуется после изменений):**
```bash
docker-compose build --no-cache
```

### Шаг 4: Запуск системы

```bash
docker-compose up -d
```

Флаг `-d` запускает контейнеры в фоновом режиме.

### Шаг 5: Проверка работы

```bash
# Проверить статус контейнеров
docker-compose ps

# Посмотреть логи узла 1
docker logs orchestrator_node_1

# Посмотреть логи узла 2
docker logs orchestrator_node_2

# Посмотреть логи балансировщика
docker logs loadbalancer
```

### Шаг 6: Открыть интерфейс

Откройте в браузере:
- **Балансировщик:** http://localhost:8080
- **Узел 1 напрямую:** http://localhost:5000
- **Узел 2 напрямую:** http://localhost:5001

---

## 🧹 ПОЛНАЯ ОЧИСТКА (Если что-то пошло не так)

Если нужно полностью очистить систему:

```bash
# 1. Остановить и удалить контейнеры
docker-compose down

# 2. Удалить все образы проекта
docker rmi distributed-compute-orchestrator_node_1 distributed-compute-orchestrator_node_2 distributed-compute-client_worker_1 distributed-compute-client_worker_2 nginx:alpine

# 3. Удалить неиспользуемые образы и кэш
docker system prune -a -f

# 4. Пересобрать с нуля
docker-compose build --no-cache

# 5. Запустить
docker-compose up -d
```

**⚠️ ВНИМАНИЕ:** Команда `docker system prune -a -f` удалит ВСЕ неиспользуемые образы, не только этого проекта!

---

## 🔍 ПРОВЕРКА УСПЕШНОЙ ПЕРЕСБОРКИ

После пересборки проверьте:

1. **Все контейнеры запущены:**
   ```bash
   docker-compose ps
   ```
   Должны быть запущены: `orchestrator_node_1`, `orchestrator_node_2`, `loadbalancer`

2. **Логи без ошибок:**
   ```bash
   docker logs orchestrator_node_1 | tail -20
   ```
   Должны быть сообщения о загрузке блокчейна и синхронизации

3. **Интерфейс открывается:**
   - Откройте http://localhost:8080
   - Должна загрузиться вкладка "Обзор" с данными узла

4. **Синхронизация работает:**
   - Проверьте `/metrics` на обоих узлах
   - Длина цепочки должна быть одинаковой после синхронизации

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: "Port already in use"
```bash
# Найти процесс, использующий порт
netstat -ano | findstr :5000  # Windows
lsof -i :5000                  # Linux/Mac

# Остановить контейнеры
docker-compose down
```

### Проблема: "Image not found"
```bash
# Пересобрать образы
docker-compose build --no-cache
```

### Проблема: "Cannot connect to Docker"
- Убедитесь, что Docker Desktop запущен
- Проверьте, что Docker daemon работает: `docker ps`

### Проблема: Старые данные не обновляются
```bash
# Остановить систему
docker-compose down

# Удалить данные (⚠️ ВНИМАНИЕ: удалит блокчейн и пользователей!)
rm -rf orchestrator_node/data/chain.json
# Или в Windows:
del orchestrator_node\data\chain.json

# Пересобрать и запустить
docker-compose up -d --build
```

---

## 📝 ЧТО ПЕРЕСБИРАЕТСЯ

При пересборке обновляются:

1. **orchestrator_node_1 и orchestrator_node_2:**
   - Код из `orchestrator_node/` (app.py, blockchain.py, contracts.py)
   - Интерфейс из `orchestrator_node/static/dashboard.html`
   - Общий модуль из `shared/`

2. **client_worker_1 и client_worker_2:**
   - Код из `client_worker/`
   - Общий модуль из `shared/`

3. **loadbalancer:**
   - Конфигурация из `nginx_loadbalancer/nginx.conf`

---

## ⚡ БЫСТРАЯ ПЕРЕСБОРКА ОДНОГО КОМПОНЕНТА

Если изменили только один компонент:

```bash
# Пересобрать только оркестратор
docker-compose build orchestrator_node_1 orchestrator_node_2
docker-compose up -d orchestrator_node_1 orchestrator_node_2

# Пересобрать только воркер
docker-compose build client_worker_1
docker-compose up -d client_worker_1

# Пересобрать только балансировщик (после изменения nginx.conf)
docker-compose up -d --force-recreate loadbalancer
```

---

## ✅ ЧЕКЛИСТ ПЕРЕСБОРКИ

- [ ] Остановлены все контейнеры (`docker-compose down`)
- [ ] Все изменения в коде сохранены
- [ ] Выполнена пересборка (`docker-compose build --no-cache`)
- [ ] Система запущена (`docker-compose up -d`)
- [ ] Проверены логи на ошибки
- [ ] Интерфейс открывается (http://localhost:8080)
- [ ] Синхронизация между узлами работает
- [ ] Данные сохраняются (блокчейн, пользователи)

---

## 🎯 РЕКОМЕНДУЕМАЯ КОМАНДА ДЛЯ ПОЛНОЙ ПЕРЕСБОРКИ

**Windows:**
```powershell
docker-compose down
docker-compose build --no-cache
docker-compose up -d
docker-compose ps
```

**Linux/Mac:**
```bash
docker-compose down && \
docker-compose build --no-cache && \
docker-compose up -d && \
docker-compose ps
```

---

Готово! После выполнения этих команд система будет полностью пересобрана с учётом всех внесённых изменений.
